# 1750 — SPlanner S1: spine (schema + Projects CRUD + dashboard skeleton)

**Commit:** 9f52d12 · **Date:** 2026-06-06

## What changed
- `apps/splanner/db.py` — splanner.db with all 5 entity tables (projects, objectives, items, checkins, digests) + enum CHECKs per locked spec; FK cascade, ranked indexes
- `apps/splanner/routes.py` — `/splanner/api` projects endpoints: list (context filter, include_archived, priority DESC), create, patch (rename/re-rank/archive), detail tree (objectives+items+checkins)
- `server/main.py` — router mount, `register_app`, SPA deep-link routes for `/splanner`
- `frontend/src/apps/splanner/` — dashboard skeleton: context tabs, ranked cards, inline create/rename/re-rank/archive
- `frontend/src/router.tsx`, `frontend/src/shared/apps.ts` — route + hub registry entries
- `tests/test_splanner_api.py` — 7 route tests incl. enum rejection + 404

## Key decisions
- Full 5-table schema lands now so later slices (S2–S6) don't migrate; only project endpoints exposed in S1
- Project `status` has no CHECK constraint — spec doesn't lock its values in this slice
- Per-request DB open/close — fine for single-user tool, no pool

## Gotchas
- Worktree carried uncommitted dev-flow work in `server/main.py`/`router.tsx`/`apps.ts` — commit was partial-staged (splanner hunks only); dev-flow remains uncommitted
- Port 7070 had a stale 8-day uvicorn master; first `lsof | head -1` PID was not the listener — verify etime after kill

## Tests
`python -m pytest tests/test_splanner_api.py -q` → 7 passed. Live smoke on :7070 — create/filter/archive/422/SPA all pass.
