# 1753 — SPlanner S4: dashboard health rollups + blocked auto-boost

**Commit:** d11298f · **Date:** 2026-06-06

## What changed
- `apps/splanner/routes.py` — GET `/api/projects` additive fields: `health` (objective status counts), `items_blocked`, `latest_checkin`, `is_blocked`; CTE-based rollup (no N+1); blocked projects ordered above priority rank
- `frontend/.../components/ProjectHealth.tsx` — HealthStripe / StackedHealthBar / ObjectiveDots
- `DashboardPage` — KPI strip (active / blocked / at-risk / done), card visuals, latest-signal line, Blocked badge
- 4 new route tests (26 total)

## Key decisions
- Auto-boost computed server-side in ORDER BY — client never re-sorts
- `is_blocked` = blocked objective OR blocked item (item-level blockage surfaces even when objectives look fine)

## Notes
Completes spec phase 1 — SPlanner usable standalone, no AI. Live smoke: lo-pri blocked project ranks above hi-pri healthy one, rollup counts correct.
